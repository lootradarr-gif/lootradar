/node_modules
/.next
/out
/build
.DS_Store
*.pem

# typescript incremental build cache
tsconfig.tsbuildinfo

# env
.env
.env*.local

# local submission store (dev only — moves to DB later)
/data

.vercel
.env*.local
